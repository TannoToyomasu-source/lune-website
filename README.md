# web-design（デザイン検証用）

本番候補の `web` のコピーです。ここで見た目・モーション・タイポを自由に試せます。

## 起動

```bash
cd "/Users/natsumi/Documents/りなさんWebサイト/web-design"
npm run dev -- -p 3005
```

- 検証用: http://localhost:3005  
- 本番候補 `web`: http://localhost:3000（別プロセス）

`web` 本体には影響しません。うまくいったら、必要な差分だけ `web` に反映してください。
